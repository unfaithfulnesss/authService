package com.example.authservice.Enums;
// this enum isnt for what type of token this is, but what it is USED for
public enum TokenType {
    REFRESH,
    PASSWORD_RESET,
    EMAIL_VERIFICATION,
    TWO_FACTOR
}
